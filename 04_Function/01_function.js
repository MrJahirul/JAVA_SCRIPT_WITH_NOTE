/*

What is function ?
=> Function is a block of code which only runs when it is called.
Example:
    const sayHello=()=>{
        console.log("Hello");
    }

    sayHello();

Parameter:When we write function defination we write parameter
Argument:When we call function we write argument

Example:
    const sayHello=(parameter)=>{
        console.log(`Hello ${name}`);
    }
    sayHello(Argument)


*/